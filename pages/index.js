export default function Home() {
  return (
    <div style={{ fontFamily: "Arial", padding: "20px" }}>
      <h2>✅ Salla Webhook Server is Running</h2>
      <p>This project only exposes API endpoints — nothing to see here.</p>
      <p>Try: <code>/api/webhook</code> or <code>/api/refresh-token</code></p>
    </div>
  );
}